**Unreleased**

* Return an action error when PostgreSQL result retrieval fails instead of reporting an empty success.

* Preserve every PostgreSQL result column by assigning stable unique names to duplicate or unnamed columns.
